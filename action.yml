name: Setup foundationDB
description: Setup a FoundationDB
branding:
  icon: database
  color: blue
inputs:
  version:
    description: FoundationDB version to install
    required: false
    default: "6.3.22"
runs:
  using: "node12"
  main: "dist/index.js"
